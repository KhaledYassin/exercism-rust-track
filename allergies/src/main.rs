fn main() {
    let number = 2;
    let shift_by = 4;
    let left_shift = number << shift_by;
    let right_shift = number >> shift_by;
    
    println!("Left shift: {} - Right shift: {}", left_shift, right_shift)
}
